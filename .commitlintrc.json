{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["feat", "fix", "docs", "style", "refactor", "test", "chore", "revert","ci"]
    ],
    "type-empty": [2, "never"],
    "scope-enum": [1, "always"],
    "scope-empty": [1, "never"]
  }
}
